{
  "name": "finprop-api",
  "version": "1.0.0",
  "description": "Pure logic financial and real estate calculation APIs",
  "scripts": {
  "build": "tsc",
  "serve": "netlify dev",
  "lint": "eslint src netlify --ext .ts",
  "lint:fix": "eslint src netlify --ext .ts --fix",
  "format": "prettier --write \"src/**/*.ts\" \"netlify/**/*.ts\""
  },
  "dependencies": {
    "date-fns": "^4.4.0",
    "lodash": "^4.18.1",
    "mathjs": "^13.0.0"
  },
  "devDependencies": {
    "@netlify/functions": "^2.0.0",
    "@types/lodash": "^4.17.0",
    "typescript": "^5"
  }
}